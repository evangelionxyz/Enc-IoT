{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Attach",
            "type": "cppdbg",
            "request": "attach",
            "program": "${workspaceFolder}/build/bin/enciot.exe",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe"
        }
    ]
}